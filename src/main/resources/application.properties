# ===============================
# DATABASE CONFIG (Neon PostgreSQL)
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}


spring.config.import=optional:file:.env[.properties]




spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# CONNECTION POOL (Recommended)
# ===============================

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000

#==============================
# Brevo SMTP Configuration
#==============================
# Brevo SMTP
#spring.mail.host=smtp-relay.brevo.com
#spring.mail.port=587
#spring.mail.username=a36733001@smtp-brevo.com
#spring.mail.password=xsmtpsib-1219bc34a3fbc3d203e67cabcc1553996145e2d8ceee92403678ad00c2a170e6-Hid3ziK92PRb5YKe

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# IMPORTANT: This must be your verified sender email in Brevo
#spring.mail.from=quipbilling@gmail.com

#==============================
# JWT CONFIG
# ===============================

jwt.secret=quipbill_secret_key_654321





#******
# Email configuration (set via environment variables)
# Use a Brevo REST API key (typically starts with xkeysib-...), not an SMTP key.
app.brevo.api-key=${BREVO_API_KEY:}
app.email.sender-email=${EMAIL_SENDER_EMAIL:}
app.email.sender-name=${EMAIL_SENDER_NAME:QuipBill}
